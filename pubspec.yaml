name: yourself_in_time_project
description: A new Flutter project.

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=2.19.5 <3.0.0'


dependencies:
  flutter:
    sdk: flutter
 
  cupertino_icons: ^1.0.2
  get: ^4.6.5
  
  lottie: ^1.4.2
  #DI
  get_it: ^7.6.0

  #UTILS
  freezed: ^2.3.4

  #STATE_MANAGEMENT
  stacked: 
  stacked_services: 

  #FIREBASE
  firebase_core: ^2.12.0
  firebase_auth: ^4.6.0
  cloud_firestore: ^4.8.1

  #SIGN
  google_sign_in: ^6.1.4
  #UI
  animations: ^2.0.1
  google_fonts: ^3.0.1
  connectivity_plus: ^4.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: 
  stacked_generator: 
  

flutter:

  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
     - assets/images/
     - assets/json/
  #   - images/a_dot_ham.jpeg



  